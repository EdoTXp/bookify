import 'package:flutter/material.dart';

class ReadingsPage extends StatelessWidget {
  const ReadingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(child: Text('Readings Page')),
    );
  }
}
